
if ( ! function_exists( 'wponion_icon_[slug]' ) ) {
	/**
	 * Returns Icons List For [name] Library
	 *
	 * @return array
	 */
	function wponion_icon_[slug]() {
		return h::read_json_file( WPONION_PATH . 'data/json/icons/[file]', true );
	}
}
